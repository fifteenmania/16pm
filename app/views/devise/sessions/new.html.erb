<%= render partial: "devise/shared/panel_s"%>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-group">
    <%= f.label :email, "이메일" %><br />
    <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "enter your email" %>
  </div>

  <div class="form-group">
    <%= f.label :password, "비밀번호" %><br />
    <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "enter your password" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-group">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, "로그인 기억하기" %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "로그인", class: "button button-default" %>
  </div>
  <% end %>
  
  <%= render "devise/shared/links" %>
<%= render partial: "devise/shared/panel_e"%>